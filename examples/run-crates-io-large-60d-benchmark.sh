#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
WORKSPACE_DIR="${ROOT_DIR}/crates-io-large-benchmark-workspace" \
CARGO_REGISTRY_GLOBAL_MIN_PUBLISH_AGE="${CARGO_REGISTRY_GLOBAL_MIN_PUBLISH_AGE:-60 days}" \
COOLDOWN_INCOMPATIBLE_PUBLISH_AGE="${COOLDOWN_INCOMPATIBLE_PUBLISH_AGE:-fallback}" \
COOLDOWN_FALLBACK_ACCEPT="${COOLDOWN_FALLBACK_ACCEPT:-auto}" \
BENCH_OFFLINE="${BENCH_OFFLINE:-0}" \
BENCH_PREFETCH_COOLDOWN="${BENCH_PREFETCH_COOLDOWN:-0}" \
  "${ROOT_DIR}/run-crates-io-benchmark.sh"
